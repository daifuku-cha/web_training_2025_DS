import React from 'react'
import { VolumeModel } from '@/models/volume.model'

type RelatedTitlesProps = {
    relatedTitles: VolumeModel
    volume: VolumeModel
}

export const relatedTitles: React.FC<RelatedTitlesProps> = (props) => {
    return (<div>
        
    </div>)
}
